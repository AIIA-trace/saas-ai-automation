# PATCH MANUAL PARA BUSINESSHOURSCONFIG
# Este patch debe aplicarse manualmente al archivo api.js en producción

# PROBLEMA: businessHoursConfig no se añade al updateData en el endpoint PUT /client

# UBICACIÓN: Después de la línea que dice:
# // email no se actualiza aquí por seguridad

# AÑADIR ESTAS LÍNEAS:

    // CRÍTICO: Añadir businessHoursConfig al updateData
    if (businessHoursFromBody) {
      updateData.businessHoursConfig = businessHoursFromBody;
      logger.info(`🕐 AÑADIENDO businessHoursConfig al updateData para cliente ${req.client.id}`);
      logger.info(`🕐 businessHoursConfig que se guardará:`, JSON.stringify(businessHoursFromBody, null, 2));
    } else {
      logger.warn(`🕐 businessHoursConfig NO se añadirá porque no se encontró en req.body`);
    }

# EXPLICACIÓN:
# 1. El endpoint extrae businessHoursConfig del req.body como businessHoursFromBody
# 2. Pero NO lo añade al updateData antes de guardar en la base de datos
# 3. Este fix añade businessHoursConfig al updateData para que se guarde
# 4. Los logs confirmarán si el fix funciona

# VERIFICACIÓN:
# Después del fix, los logs deberían mostrar:
# - "🕐 AÑADIENDO businessHoursConfig al updateData para cliente 3"
# - "🕐 businessHoursConfig que se guardará: {...}"

# Si no aparecen estos logs, el fix no se aplicó correctamente
