/**
 * Dynamic Dashboard Overview Module
 * Generates sector-specific dashboard based on company configuration
 */

document.addEventListener('DOMContentLoaded', function() {
    const overviewTab = document.getElementById('overview');
    if (!overviewTab) return;
    
    // Initialize dashboard overview
    window.dashboardOverview = new DashboardOverview();
});

// Rest of the code remains the same...

// Dashboard Overview Module - Dinámico basado en configuración empresarial
class DashboardOverview {
    // ... (rest of the code remains the same)
}
